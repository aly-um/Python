""" Creating a frequency table with pandas
This method uses groupby() and .size() to create a frequency table
"""

# Import packages and functions
import pandas as pd

# Load the dataset
df = pd.read_csv('credit_card.csv')

# Create a frequency table for Total_calls_made
freqTable = df.groupby(by=["Total_calls_made"]).size()

# Display frequency table
print(freqTable)



""" Creating a contingency table with pandas
This method uses pivot_table() to create a contingency table
"""

# Import packages and functions
import pandas as pd
import numpy as np

# Load the dataset
df = pd.read_csv('credit_card.csv')

# Create a contingency table for Total_visits_bank and Total_visits_online
contingencyTable = df.pivot_table(values="Customer Key", index="Total_visits_bank",
 columns="Total_visits_online", aggfunc=np.size)

# Display contingency table
print(contingencyTable)
